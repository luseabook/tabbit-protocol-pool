# 13-真实协议校准与端到端验收

本文记录真实 Tabbit send endpoint / session verify endpoint / 签名 / 请求体校准结果，以及继续做 Codex / Claude Code 真实端到端验证、工具 loop 取舍和 403 响应体细分的验收流程。


## 0. 本地 readiness 预检

在跑真实网络探针前，先执行离线预检：

~~~powershell
node bin/tabbit-pool.js readiness --json
~~~

该命令只读取本地账号元数据、protocol probe fixture 摘要和本地 `readiness.json` 验收标记，不触发 Tabbit 网络。它会告诉你四项是否 ready：

1. `protocolCalibration`：是否已配置 protocol、sendPath、sessionVerifyPath、active 账号、成功 verifySession fixture 和成功 sendMessage fixture。
2. `codexClaudeE2E`：Codex / Claude Code 人工验收是否已记录。
3. `toolLoopDecision`：当前工具 loop 策略，默认客户端先执行工具，也可显式关闭或启用受控本地 loop。
4. `forbidden403`：是否已有 403/forbidden 脱敏 fixture。

`status:"blocked"` 时先按 `nextActions[]` 逐项补齐。Codex 或 Claude Code 真实端到端验收完成后，用下面命令写入本地标记：

~~~powershell
node bin/tabbit-pool.js readiness mark --codex-verified --json
node bin/tabbit-pool.js readiness mark --claude-verified --json
# 两项都刚完成时也可以一次写入
node bin/tabbit-pool.js readiness mark --codex-verified --claude-verified --json
~~~

标记写入 `TABBIT_POOL_STATE_DIR/readiness.json`，只记录 `verified:true` 和 `verifiedAt`，不保存 prompt、代码 diff、cookie 或 API key。

### 0.1 外部脱敏 evidence state 诊断

默认状态目录通常位于用户本地应用数据目录。如果该目录没有账号元数据、`readiness.json` 或 protocol probe fixture，`readiness` / `fixtures audit` 返回 `blocked` 是安全默认，不代表实现失败。要复用本机已经存在的脱敏 evidence state，应显式选择外部状态目录，再运行只读诊断：

~~~powershell
$env:TABBIT_POOL_STATE_DIR = "E:\tabbit2api\output\tabbit-live-state"
$env:TABBIT_POOL_PROTOCOL_ENABLED = "true"
$env:TABBIT_POOL_PROTOCOL_BASE_URL = "https://web.tabbit.ai"
$env:TABBIT_POOL_PROTOCOL_SEND_PATH = "/api/v1/chat/completion"
$env:TABBIT_POOL_PROTOCOL_SESSION_VERIFY_PATH = "/api/v0/user/base-info"
$env:TABBIT_POOL_COMPAT_STRIP_CLIENT_TOOLS = "true"

node bin\tabbit-pool.js readiness doctor --json
node bin\tabbit-pool.js readiness --json
node bin\tabbit-pool.js fixtures audit --json
node bin\tabbit-pool.js fixtures audit --scope session --json
node bin\tabbit-pool.js fixtures audit --scope upstream --json
~~~

`readiness doctor` 只读取当前 `stateDir` 下的账号摘要、脱敏 fixture 和 `readiness.json`，并返回协议 env 是否配置、readiness 状态、默认 fixture audit 状态、core 剩余工作、auth/benefits/session/upstream `calibrationBacklog` 和安全命令。它不会调用 `accounts probe`、不会运行 `probe protocol`、不会写 `readiness.json`，也不会输出 cookie、session、API key、`cookieJarRef`、`TABBIT_POOL_PROTOCOL_REQ_CTX` 或 raw fixture payload。

注意 `remainingWork: []` 只代表基础 gateway/chat readiness gate 没有剩余阻塞；注册/登录、M05 副作用、session lifecycle 和真实上游 stream boundary evidence 由 `calibrationBacklog` 跟踪。若 `calibrationBacklog.status` 仍为 `blocked`，必须继续运行对应 `fixtures audit --scope auth --json` / `fixtures audit --scope benefits --json` / `fixtures audit --scope session --json` / `fixtures audit --scope upstream --json` 和安全 probe 流程。`calibrationBacklog.captureCommands` 和非 JSON doctor 的 `capture_command` 行可作为下一步命令提示，但只包含 `<account-id>` / `<redacted-input.json>` 占位符，并按 template、validate、confirm_validate、probe 四步排列；真实副作用 probe 前必须先审查 input file 和 `confirmSideEffect`，先运行 `probe validate --operation <operation> --input-file <redacted-input.json> --json` 只读检查 schema、字段存在性和 body key 摘要，准备写入副作用 fixture 前再运行 `confirm_validate` 对应命令确认 `confirmSideEffect:true`，最后才决定是否运行 `probe protocol --write-fixture`。真实用券消耗缺口现在走 `useResetCoupon` template/validate/confirm_validate/probe 流，并要求 `TABBIT_POOL_PROTOCOL_BENEFIT_COUPON_USE_PATH`；离线 `consumeResetCoupon` 仅用于外部脱敏 endpoint/body/result hash evidence 导入。自动刷新仍只提供离线 `recoverSession` template/validate evidence 流且 `probe` 为空；离线 evidence 经人工审查后可在同一 validate 命令上追加 `--write-fixture` 写入白名单脱敏 fixture，不读取账号、secret 或网络，也不会保留 input 中的额外 raw payload 字段。真实上游 stream boundary 在已配置 send path 后会给出 sendMessage template/validate/probe 命令，但 audit 仍要求脱敏真实上游 evidence marker。session scope 的恢复策略当前仍是 `manual_reimport_then_probe`，自动刷新路径未校准。

对 `successful_reset_coupon_consumption_fixture` 和 `automated_session_refresh_strategy`，JSON doctor 会直接输出 `writeFixtureCommand`，plain `capture_command` 会输出 `write_fixture=`。这两个命令等价于在已通过人工审查的离线 evidence `probe validate` 命令上追加 `--write-fixture`，只写白名单脱敏 fixture；其它 capture command 不会生成该字段，仍必须按真实协议 probe 或 fixture audit gate 校准。

外部 evidence state 的边界：

- raw 抓包、浏览器 profile、`tabbit-cookie.txt` 和未审查输出仍只留本机，不进入仓库。
- `stateDir/fixtures/protocol-probes/*.json` 必须是 sanitizer 写出的 protocol probe fixture，不能手工粘贴 raw 请求/响应。
- `stateDir/readiness.json` 只记录 Codex / Claude Code 人工验收标记，不代表 endpoint 自动校准成功。
- `fixtures audit` 负责证据覆盖；`readiness` 负责上线阻塞快照；两者都只读本地状态，不触发真实网络。
- 外部 `TABBIT_POOL_STATE_DIR` 可读但不可写时，可以设置 `TABBIT_POOL_PROTOCOL_FIXTURE_DIR` 到独立可写目录承接脱敏 fixture；账号、secret 和 `readiness.json` 仍来自 `TABBIT_POOL_STATE_DIR`。

## 总体原则

- 不猜真实 endpoint：所有真实路径必须来自本机抓包或 `probe protocol` 脱敏 fixture。
- 不提交 raw secret：cookie、session、token、验证码、真实 API key 只留在本地 state/secret store。
- 先 fixture，后 runtime：每次调整签名、请求体、响应解析前，先保存脱敏样本并补回归测试。
- 先本地回环，后客户端：先确认 `/health`、`/v1/models`、`/v1/chat/completions`、`/v1/responses`、`/v1/messages` 本地 curl/fetch 可用，再跑 Codex / Claude Code。

## 1. 真实 Tabbit send/session endpoint / 签名 / 请求体校准

当前已校准：

- 页面域名与协议 base URL：`https://web.tabbit.ai`。
- 真实发送接口：`POST /api/v1/chat/completion`。
- 真实会话验证接口：`GET /api/v0/user/base-info`。
- 模型目录接口：`GET /proxy/v1/model_config/models?a=0&scene=chat`。
- 额度 usage 查询接口：`GET /api/commerce/quota/v1/usage?user_id=<user_id>`。
- 只读 commerce 状态/资源接口：
  - `GET /api/commerce/activity/v1/lottery/me`
  - `GET /api/commerce/activity/v1/newbie-exploration/me`
  - `GET /api/commerce/placement/v1/resources?placement_code=home.input_below`
  - `GET /api/commerce/reward/v1/card-records?user_id=<user_id>`
  - `GET /api/commerce/lottery/v1/hit-records?user_id=<user_id>`
- M05 显式 probe 接口：
  - `GET /api/commerce/activity/v1/sign-in/status?scene_codes=desktop_pet`
  - `POST /api/commerce/activity/v1/sign-in`
  - `GET /api/commerce/benefit/v1/coupon/list?user_id=<user_id>&coupon_type=weekly_reset_coupon`
  - `POST /api/commerce/activity/v1/participate`
  - `GET /api/commerce/product/v1/sku/usage-reset-coupon`
  - `GET /api/commerce/lottery/v1/available-chances?user_id=<user_id>&activity_id=<activity_id>`
  - `GET /api/commerce/lottery/v1/active-main-pools?activity_id=<activity_id>`
  - `GET /api/commerce/activity/v1/lottery/chance-records?activity_id=<activity_id>`
  - `POST /api/commerce/lottery/v1/draw`
- `Default` 是当前账号可用的 `free_unlimited` 模型，`tabbit/priority` 会映射到 `Default`。
- 浏览器签名公式：`x-nonce = HMAC-SHA256(signKey, "${timestamp}.${x-signature}.${sha256(bodyText)}")`，其中 `x-signature` 为 UUID/randomUUID 风格字符串。
- `GET /api/v0/user/base-info` 成功响应包含 `success:true` 和 `user_info.id`；无效 cookie 返回 401 和登录提示，归类为 `login_required` / `accountStatus:"login_expired"`。
- quota usage 成功响应包含 `member_level`、`usage_percentage`、`remaining_reset_hours`、`current_cycle_start`、`current_cycle_end`、`unused_reset_coupon_count` 等字段；请求需要已登录 Cookie 和 `unique-uuid`，当前 evidence 未观察到 `x-signature` / `x-nonce`。
- 只读 commerce GET 需要已登录 Cookie、`accept: application/json`、`unique-uuid` 和可选 `x-req-ctx`，当前 evidence 未观察到 `x-signature` / `x-nonce`；`newbie-exploration.view_mode` 合法值为 `event_gate`、`float_collapsed`、`float_expanded`、`activity_page`；`placement_resources` 已验证默认 `placement_code=home.input_below`。
- M05 side-effect probe 来自生产 chunk 与 live invalid-body/invalid-business 回放：签到/抽奖类 POST 使用 `trace-id + Content-Type`；reset-coupon activity participate 使用 `x-req-ctx + unique-uuid + Content-Type`；当前 evidence 未观察到 `x-signature` / `x-nonce`。这些 POST 在实现中要求 `confirmSideEffect:true`；其中每日签到已验证，显式配置 `TABBIT_POOL_PROTOCOL_SIGN_IN_PATH` 后可由默认 `maintain` 自动执行，其它 POST 不会被默认 `maintain` 自动调用。
- 消息内已上传附件引用结构：document 使用 `{ type:"document", title, content:"", metadata:{ file_id } }`，image 使用 `{ type:"image", title, content, metadata:{ file_id, source_url? } }`。
- 附件上传真实链路：`POST /proxy/v0/cos/presigned-upload-url`、`PUT <presigned COS url>`、`POST /api/v0/cos/complete-upload`；完整配置两个 Tabbit path 后，runtime 会在真实发送前自动上传 raw/base64 附件并引用 file id。
- `/session/:id` + Next action 是会话分页/history，不是发送接口。

### 1.1 准备账号和状态目录

确认账号已经导入到本地账号 store，并且账号有 `cookieJarRef` 指向本地 secret。真实注册/登录 endpoint 未还原前，先用已登录账号导入：

~~~powershell
# 推荐：把浏览器/客户端里的 Cookie header 保存到文件，避免 shell history 留 cookie
node bin/tabbit-pool.js accounts import-session --id acct_default --email user@example.test --access-tier pro --cookie-file .\tabbit-cookie.txt --json

node bin/tabbit-pool.js accounts list --json
node bin/tabbit-pool.js accounts probe acct_default --json
~~~

如果 `accounts probe` 返回 `session_missing`，先刷新或重新导入 session。

### 1.2 显式配置协议路径

真实路径建议显式配置，尤其是默认会话 ID：

~~~powershell
$env:TABBIT_POOL_PROTOCOL_ENABLED = "true"
$env:TABBIT_POOL_PROTOCOL_BASE_URL = "https://web.tabbit.ai"
$env:TABBIT_POOL_PROTOCOL_SIGN_KEY_PATH = "/chat/sign-key"
$env:TABBIT_POOL_PROTOCOL_SEND_PATH = "/api/v1/chat/completion"
$env:TABBIT_POOL_PROTOCOL_ATTACHMENT_UPLOAD_PATH = "/proxy/v0/cos/presigned-upload-url"
$env:TABBIT_POOL_PROTOCOL_ATTACHMENT_COMPLETE_UPLOAD_PATH = "/api/v0/cos/complete-upload"
$env:TABBIT_POOL_PROTOCOL_MODEL_CATALOG_PATH = "/proxy/v1/model_config/models"
$env:TABBIT_POOL_PROTOCOL_MODEL_CATALOG_SCENE = "chat"
$env:TABBIT_POOL_PROTOCOL_CHAT_SESSION_ID = "<已登录页面中的 session uuid>"
$env:TABBIT_POOL_PROTOCOL_REQ_CTX = "MS4zLjI2KDEwMTAzMDI2KQ=="
$env:TABBIT_POOL_PROTOCOL_SESSION_VERIFY_PATH = "/api/v0/user/base-info"
$env:TABBIT_POOL_PROTOCOL_QUOTA_USAGE_PATH = "/api/commerce/quota/v1/usage"
$env:TABBIT_POOL_PROTOCOL_ACTIVITY_LOTTERY_PATH = "/api/commerce/activity/v1/lottery/me"
$env:TABBIT_POOL_PROTOCOL_NEWBIE_EXPLORATION_PATH = "/api/commerce/activity/v1/newbie-exploration/me"
$env:TABBIT_POOL_PROTOCOL_PLACEMENT_RESOURCES_PATH = "/api/commerce/placement/v1/resources"
$env:TABBIT_POOL_PROTOCOL_REWARD_CARD_RECORDS_PATH = "/api/commerce/reward/v1/card-records"
$env:TABBIT_POOL_PROTOCOL_LOTTERY_HIT_RECORDS_PATH = "/api/commerce/lottery/v1/hit-records"
$env:TABBIT_POOL_PROTOCOL_SIGN_IN_STATUS_PATH = "/api/commerce/activity/v1/sign-in/status"
$env:TABBIT_POOL_PROTOCOL_SIGN_IN_PATH = "/api/commerce/activity/v1/sign-in"
$env:TABBIT_POOL_PROTOCOL_BENEFIT_COUPON_LIST_PATH = "/api/commerce/benefit/v1/coupon/list"
$env:TABBIT_POOL_PROTOCOL_ACTIVITY_PARTICIPATE_PATH = "/api/commerce/activity/v1/participate"
$env:TABBIT_POOL_PROTOCOL_USAGE_RESET_COUPON_SKU_PATH = "/api/commerce/product/v1/sku/usage-reset-coupon"
$env:TABBIT_POOL_PROTOCOL_LOTTERY_AVAILABLE_CHANCES_PATH = "/api/commerce/lottery/v1/available-chances"
$env:TABBIT_POOL_PROTOCOL_LOTTERY_ACTIVE_MAIN_POOLS_PATH = "/api/commerce/lottery/v1/active-main-pools"
$env:TABBIT_POOL_PROTOCOL_LOTTERY_CHANCE_RECORDS_PATH = "/api/commerce/activity/v1/lottery/chance-records"
$env:TABBIT_POOL_PROTOCOL_LOTTERY_DRAW_PATH = "/api/commerce/lottery/v1/draw"
~~~

真实 Tabbit 页面在当前本地环境需要代理；Node 探针也应使用同一个本机代理：

~~~powershell
$env:NODE_USE_ENV_PROXY = "1"
$env:HTTPS_PROXY = "http://127.0.0.1:7890"
$env:HTTP_PROXY = "http://127.0.0.1:7890"
~~~

`TABBIT_POOL_PROTOCOL_SESSION_VERIFY_PATH` 已校准为 `/api/v0/user/base-info`，但仍保持显式配置；留空时 `accounts probe` 会返回 `protocol_missing` 或 skipped，这是安全行为。

`TABBIT_POOL_PROTOCOL_QUOTA_USAGE_PATH` 已校准为 `/api/commerce/quota/v1/usage`，但也保持显式配置；留空时默认 `maintain` 不会触发真实额度查询，只会输出 skipped 维护动作。

activity/newbie/placement/reward/lottery path 已校准为只读 commerce 状态/资源探针，M05 side-effect path 已校准为显式 probe；这些 path 都保持显式配置。留空时对应 `probe protocol --operation ...` 会返回 protocol_missing 或 skipped。配置 `TABBIT_POOL_PROTOCOL_SIGN_IN_PATH` 会让默认 `maintain` 执行已验证每日签到；生产建议同时配置 `TABBIT_POOL_PROTOCOL_SIGN_IN_STATUS_PATH`，让维护先查 `signedToday`。配置其它 side-effect path 不会让默认 `maintain` 自动领取 Pro、抽奖或使用重置券；POST 型方法还要求 `confirmSideEffect:true`。

注册/登录 auth 入口已具备显式配置，但真实 path/body 仍未完成 success evidence 校准。只有拿到自动浏览器或协议捕获的安全证据后，才应设置：

~~~powershell
$env:TABBIT_POOL_PROTOCOL_AUTH_SEND_CODE_PATH = "<verified send-code path>"
$env:TABBIT_POOL_PROTOCOL_AUTH_SEND_CODE_METHOD = "POST"
$env:TABBIT_POOL_PROTOCOL_AUTH_SUBMIT_CODE_PATH = "<verified submit-code path>"
$env:TABBIT_POOL_PROTOCOL_AUTH_SUBMIT_CODE_METHOD = "POST"
~~~

`ProtocolTabbitClient.sendVerificationCode()` 的默认 body `{ "email": "<email>" }`、`submitRegistrationOrLogin()` 的默认 body `{ "email": "<email>", "code": "<code>" }` 只是校准占位，不是已确认真实协议。真实 body 若包含 scene、captcha、request id、csrf 或其它字段，必须通过显式 captured body 输入，并补脱敏 fixture 与回归测试。fixture 不得包含真实验证码、cookie、session、JWT、Bearer token、raw payload、prompt 或真实用户数据。

auth endpoint/body evidence 可走和其它协议探针一致的脱敏 fixture pipeline。模板默认不确认副作用：

~~~powershell
node bin/tabbit-pool.js probe template --operation sendVerificationCode --json > .\probe-auth-send-code.json
node bin/tabbit-pool.js probe template --operation submitRegistrationOrLogin --json > .\probe-auth-submit-code.json
~~~

人工对照自动浏览器或协议捕获结果编辑本地 input 文件，只保留可复核的字段名和占位值；确认账号、邮箱、验证码和目标 endpoint 都是安全可测后，先做只读预检，再把 `confirmSideEffect` 改为 `true` 并做 strict 预检：

~~~powershell
node bin/tabbit-pool.js probe validate --operation sendVerificationCode --input-file .\probe-auth-send-code.json --json
node bin/tabbit-pool.js probe validate --operation submitRegistrationOrLogin --input-file .\probe-auth-submit-code.json --json
node bin/tabbit-pool.js probe validate --operation sendVerificationCode --input-file .\probe-auth-send-code.json --require-confirmed-side-effect --json
node bin/tabbit-pool.js probe validate --operation submitRegistrationOrLogin --input-file .\probe-auth-submit-code.json --require-confirmed-side-effect --json
~~~

strict 预检仍不读取账号、secret、fixture，也不触网；它只确认已知副作用 operation 的 input 文件已经显式设置 `confirmSideEffect:true`。通过后才执行：

~~~powershell
node bin/tabbit-pool.js probe protocol --account acct_default --operation sendVerificationCode --input-file .\probe-auth-send-code.json --write-fixture --json
node bin/tabbit-pool.js probe protocol --account acct_default --operation submitRegistrationOrLogin --input-file .\probe-auth-submit-code.json --write-fixture --json
~~~

`probe protocol` 会先校验 `email`、`code`、`body` 和 `confirmSideEffect`；缺少 `confirmSideEffect:true` 时不会读取 secret 或调用协议 client。auth 探针仍需要 `--account` 指向本地账号元数据以记录归属，但可以使用没有 session secret 的 provisioning 账号。生成的 fixture 仍需人工检查确认只包含脱敏结构，不提交真实验证码、cookie、session、JWT、Bearer token、raw payload、prompt、真实用户数据或本地 state 目录。

auth evidence 单独审计：

~~~powershell
node bin/tabbit-pool.js fixtures audit --scope auth --json
~~~

该命令只读本地脱敏 protocol probe fixture，检查成功 `sendVerificationCode` 和成功 `submitRegistrationOrLogin` 覆盖；它不会触发网络，也不会改变默认 `fixtures audit --json` 的 verify/send/stream/tool/403 readiness 语义。

`sendVerificationCode` 不能只凭 2xx、`ok:true` 或泛 `status/result:"success"` 算闭环。auth scope 会保留 `counts.successfulSendVerificationCode` 作为 transport success 数，但 `successful_sendVerificationCode_fixture` coverage 要求成功 fixture 里存在 send/delivery/verification/code-send 专属成功字段，例如 `codeSent:true`、`emailSent:true` 或 `sendResult:"sent"`。

`submitRegistrationOrLogin` 不能只凭 2xx 或 `ok:true` 算闭环。auth scope 的 submit coverage 要求成功 fixture 的响应形状仍能证明存在 `cookieHeader`、`cookieJar`、`cookie`、`session`、`sessionToken` 或 `token` 等可导入 session material 字段；这些字段的值必须保持脱敏，不能提交 raw cookie/session/JWT/Bearer token。

M05 side-effect evidence 单独审计：

~~~powershell
node bin/tabbit-pool.js fixtures audit --scope benefits --json
~~~

该命令只读本地脱敏 protocol probe fixture，检查成功 `dailySignIn`、活动 Pro 成功、真实重置券消耗和抽奖成功覆盖；它不会触发网络，不会领取权益、用券或抽奖，也不会改变默认 `fixtures audit --json` 的 readiness 语义。每日签到接受 `signInResult:"success"`、`signedToday:true` 或 `signInResult:"already_signed"`；`already_signed` 只说明当天签到已完成，不代表重复发放额度，也不影响其它副作用成功规则。活动 Pro 成功必须有 participation/activity/claim/pro 专属成功字段，不能只凭 2xx、`ok:true` 或泛 `status/result:"success"` 闭环。`participateResetCouponActivity` 只说明重置券活动参与状态，即使响应包含 consumed/used 字段也不计为真实重置券消耗成功；`sendMessage`、`verifySession` 等无关 fixture 不会进入 benefits scope 的读取或 `total/success/failed` 计数。

`successful_reset_coupon_consumption_fixture` 的 doctor capture command 只提供离线 evidence 模板和校验：

~~~powershell
node bin/tabbit-pool.js probe template --operation consumeResetCoupon --json > .\probe-consumeResetCoupon.json
node bin/tabbit-pool.js probe validate --operation consumeResetCoupon --input-file .\probe-consumeResetCoupon.json --json
node bin/tabbit-pool.js probe validate --operation consumeResetCoupon --input-file .\probe-consumeResetCoupon.json --write-fixture --json
~~~

`consumeResetCoupon` 不是网络探针 operation；`probe protocol --operation consumeResetCoupon` 会被 CLI 拒绝。该 evidence 需要 `reset_coupon_consumption_evidence`、消费类 operation、`status:"success"`、脱敏 `endpointHash` / `bodyHash` / `resultHash`、`safe:true`、`sanitized:true`、`rawPayload:false` 和真实消费成功信号。`--write-fixture` 只写入校验后的白名单字段和脱敏 hash evidence，不写入 input 文件中的额外 raw payload、prompt、cookie、session 或 token。只有当安全 fixture 被写入 stateDir，并通过 `fixtures audit --scope benefits --json` 计入 `successfulResetCouponConsumption` 后，真实用券消耗才可视为 ready。

session lifecycle evidence 单独审计：

~~~powershell
node bin/tabbit-pool.js fixtures audit --scope session --json
~~~

该命令只读本地脱敏 protocol probe fixture，检查成功 `verifySession`、上游 401/login_required 过期 evidence，以及显式脱敏 session recovery strategy evidence。`session_missing` 只说明本地 secret 不存在，会单独计数，但不计为上游过期。输出包含 `lifecycle` 时间窗口和 `recoveryStrategy`；在捕获安全刷新或 re-auth 路径前，`automatedRefresh` 保持 `not_calibrated`，并以 `automated_session_refresh_strategy` 让 session scope 继续 blocked。成功/过期 lifecycle coverage ready 只证明过期可观测，不代表账号恢复策略已可用；恢复策略 evidence 必须显式声明安全、已脱敏且不含 raw payload。

`automated_session_refresh_strategy` 的 doctor capture command 只提供离线 evidence 模板和校验：

~~~powershell
node bin/tabbit-pool.js probe template --operation recoverSession --json > .\probe-recoverSession.json
node bin/tabbit-pool.js probe validate --operation recoverSession --input-file .\probe-recoverSession.json --json
node bin/tabbit-pool.js probe validate --operation recoverSession --input-file .\probe-recoverSession.json --write-fixture --json
~~~

`recoverSession` 不是网络探针 operation；`probe protocol --operation recoverSession` 会被 CLI 拒绝。`--write-fixture` 只写入 `session_recovery_strategy` 的白名单 evidence 字段，不写入 input 文件中的 raw cookie/session/token/prompt。只有当安全 re-auth 或 refresh 路径的脱敏 `session_recovery_strategy` / `recoverSession` fixture 被写入 stateDir，并通过 `fixtures audit --scope session --json` 计入 `recoveryStrategyEvidence` 后，session 恢复策略才可视为 ready。

### 1.3 执行真实 session verify 探针并写 fixture

~~~powershell
node bin/tabbit-pool.js probe protocol --account acct_default --operation verifySession --write-fixture --json
~~~

成功或失效样本都应保留脱敏 fixture。成功样本用于 readiness 的 `successfulVerifySessionFixtures` 证据；401 样本用于确认失效 cookie 会走 `login_required` 分类。fixture 中不能包含 raw cookie、session、token 或 `cookieJarRef`。

### 1.4 生成最小 sendMessage 探针输入

~~~powershell
node bin/tabbit-pool.js probe template --operation sendMessage --json > .\probe-sendMessage.json
~~~

默认模板：

~~~json
{
  "model": "tabbit/priority",
  "messages": [
    { "role": "user", "content": "ping" }
  ],
  "stream": true
}
~~~

`stream:true` 是默认值，用于让同一 input 文件可直接进入真实上游 error-frame、cancellation 和 backpressure 捕获流程；如果只需要非流式文本探针，可人工改为 `stream:false`。也可以加 `tools` 或工具回合输入，但第一次校准建议从最小文本请求开始。

### 1.5 执行真实协议探针并写 fixture

~~~powershell
node bin/tabbit-pool.js probe protocol --account acct_default --operation sendMessage --input-file .\probe-sendMessage.json --write-fixture --json
~~~

成功或失败都应保留脱敏 fixture。查看：

~~~powershell
node bin/tabbit-pool.js fixtures list --json
node bin/tabbit-pool.js fixtures audit --json
node bin/tabbit-pool.js fixtures show fixtures/protocol-probes/<ref>.json --json
~~~

`fixtures audit` 会检查上线前五类证据是否齐全：成功 `verifySession`、成功 `sendMessage`、流式文本、工具调用或明确不支持原生工具字段的证据、403/forbidden 响应。它只读本地脱敏 fixture，不触发网络，也不输出 raw payload。

注册/登录、M05 副作用、session lifecycle 和真实上游 stream boundary evidence 使用独立 scope 审计：`fixtures audit --scope auth --json`、`fixtures audit --scope benefits --json`、`fixtures audit --scope session --json` 和 `fixtures audit --scope upstream --json`。这些 scope 只用于显示校准缺口，不会让默认 readiness 因缺少 auth/benefits/session/upstream fixture 而 blocked；`readiness doctor --json` 会把四者合并到 `calibrationBacklog`，避免基础 `remainingWork` 清零时掩盖剩余真实协议校准工作。

`fixtures audit --scope upstream --json` 只读取脱敏 `sendMessage` fixture，并要求样本同时带有显式真实上游 evidence marker 与 stream/SSE/NDJSON 元数据，才会把 `real_upstream_error_frame_fixture`、`real_upstream_cancellation_fixture` 或 `real_upstream_backpressure_fixture` 判为 ready。非流式 `protocol_probe` sendMessage 可满足默认 send readiness，但不满足 upstream stream boundary readiness；本地 HTTP route adapter 测试、compat handler 测试和单测 fake `ReadableStream` 只证明本地实现边界，不满足真实 upstream scope；真实 Tabbit SSE 错误帧、断开传播和 first-token flush/backpressure 仍需由安全可复现的脱敏 protocol probe fixture 闭环。

当前已用本地 `acct_default` 和 `Default` 模型拿到成功 `sendMessage` fixture，并先保存到 `output/` 下的临时 state，未提交 raw 抓包、cookie 或本地 fixture。后续若要把 fixture 纳入仓库回归，应只提交脱敏后的最小样本。

### 1.5.1 已上传附件引用探针

真实附件引用已通过本地脱敏 evidence 校准：`output/tabbit-attachment-live-calibration/attachment-upload-send.sanitized.json`。该 evidence 只保留可复核的结构化结论，不包含 raw cookie、token、profile 或原始文件内容。

发送体中的附件引用结构：

~~~json
{
  "references": [
    {
      "type": "document",
      "title": "guide.md",
      "content": "",
      "metadata": { "file_id": "<uploaded file id>" }
    },
    {
      "type": "image",
      "title": "diagram.png",
      "content": "<optional url/content>",
      "metadata": {
        "file_id": "<uploaded file id>",
        "source_url": "<optional source url>"
      }
    }
  ]
}
~~~

当前 runtime 支持把已上传附件引用从 `sendMessage({ attachments })` 映射到上述结构。附件对象提供 `path`、`file_id`、`fileId`、`id` 或 `metadata.file_id` 时不会重复上传；缺少 file id/path 但包含 `data`、`base64`、`raw` 或 `body`，且已配置 `TABBIT_POOL_PROTOCOL_ATTACHMENT_UPLOAD_PATH=/proxy/v0/cos/presigned-upload-url` 与 `TABBIT_POOL_PROTOCOL_ATTACHMENT_COMPLETE_UPLOAD_PATH=/api/v0/cos/complete-upload` 时，会先执行“预签名 -> COS PUT -> complete-upload”，再把 file id 写入发送请求。未完整配置上传链时仍返回 `unsupported_feature/ATTACHMENT_REFERENCE_REQUIRED`，不会丢弃附件后降级成纯文本发送。

### 1.5.2 真实 quota usage 探针

真实额度 usage 已通过本地脱敏 evidence 校准：`output/tabbit-live-state` 下的 quota usage 探针结果只保留结构化响应，不包含 raw cookie、token、profile 或 user session。

最小探针：

~~~powershell
node bin/tabbit-pool.js probe template --operation refreshQuota --json > .\probe-refreshQuota.json
node bin/tabbit-pool.js probe protocol --account acct_default --operation refreshQuota --input-file .\probe-refreshQuota.json --write-fixture --json
~~~

默认模板是 `{}`，runner 会使用账号元数据中的 `userId`；也可以显式传入：

~~~json
{
  "userId": "<tabbit user id>"
}
~~~

真实请求结构：

~~~text
GET https://web.tabbit.ai/api/commerce/quota/v1/usage?user_id=<user_id>
accept: application/json
x-req-ctx: <optional>
unique-uuid: <browser uuid style value>
Cookie: <hydrated local session>
~~~

当前校准结论：

- 不请求 sign-key。
- 不携带 `x-signature` 或 `x-nonce`。
- `member_level` 归一化为账号 `accessTier`。
- `usage_percentage` 归一化为 `quotaState[0].usagePercentage`，`unit` 为 `usage_percentage`。
- `current_cycle_end` 归一化为 `quotaState[0].resetAt`。
- `unused_reset_coupon_count` 归一化为 `resetCouponCount`。
- 该接口只查询 usage 状态，不代表签到、活动 Pro 领取、抽奖或重置券使用会进入默认维护链。

启用默认维护任务时：

~~~powershell
node bin/tabbit-pool.js maintain --json
~~~

显式配置 `TABBIT_POOL_PROTOCOL_QUOTA_USAGE_PATH` 后，默认 maintainer 会读取本地 `cookieJarRef` 并执行真实 `refreshQuota`。显式配置 `TABBIT_POOL_PROTOCOL_SIGN_IN_PATH` 后，默认 maintainer 会执行已验证 `dailyCheckin`；若同时配置 `TABBIT_POOL_PROTOCOL_SIGN_IN_STATUS_PATH`，会先查 `signedToday`，已签则不再 POST。其它已校准的 M05 side-effect 方法只作为显式 `probe protocol` / gateway client 方法暴露；`claimProIfAvailable` 和 `useResetCoupon` 在默认 maintainer 中仍保持 skipped，不能用未验证成功语义的 endpoint 代替。

### 1.5.3 真实只读 commerce 状态/资源探针

真实 activity/newbie/placement/reward/lottery 五类只读 commerce 接口已通过本地脱敏 evidence 校准；其中 placement resources 来自浏览器自动拦截 evidence。临时 evidence 位于 `output/tabbit-benefits-recon/` 和 `output/tabbit-benefits-browser-intercept/`，只保留状态码、路径、query 形状和脱敏响应摘要，不包含 raw cookie、token、profile 或真实 session。

最小探针：

~~~powershell
node bin/tabbit-pool.js probe protocol --account acct_default --operation getLotteryExplorationMe --write-fixture --json
node bin/tabbit-pool.js probe protocol --account acct_default --operation getNewbieExplorationMe --input-json '{"viewMode":"activity_page","includeCompletions":true,"includeRewards":true}' --write-fixture --json
node bin/tabbit-pool.js probe protocol --account acct_default --operation getPlacementResources --input-json '{"placementCode":"home.input_below"}' --write-fixture --json
node bin/tabbit-pool.js probe protocol --account acct_default --operation listRewardCardRecords --input-json '{"offset":0,"limit":10}' --write-fixture --json
node bin/tabbit-pool.js probe protocol --account acct_default --operation listLotteryHitRecords --input-json '{"offset":0,"limit":20}' --write-fixture --json
~~~

真实请求结构：

~~~text
GET https://web.tabbit.ai/api/commerce/activity/v1/lottery/me
GET https://web.tabbit.ai/api/commerce/activity/v1/newbie-exploration/me?view_mode=activity_page&include_completions=true&include_rewards=true
GET https://web.tabbit.ai/api/commerce/placement/v1/resources?placement_code=home.input_below
GET https://web.tabbit.ai/api/commerce/reward/v1/card-records?user_id=<user_id>&offset=0&limit=10&order.field=award_time&order.order=desc
GET https://web.tabbit.ai/api/commerce/lottery/v1/hit-records?user_id=<user_id>&offset=0&limit=20
accept: application/json
x-req-ctx: <optional>
unique-uuid: <browser uuid style value>
Cookie: <hydrated local session>
~~~

当前校准结论：

- 这些接口均为只读 GET，不请求 sign-key。
- 不携带 `x-signature` 或 `x-nonce`。
- `newbie-exploration` 的 `view_mode` 合法值为 `event_gate`、`float_collapsed`、`float_expanded`、`activity_page`。
- placement resources 使用 `placement_code` query；`home.input_below` 是当前已验证默认值，不应写死为唯一永久值。
- 记录查询需要 `user_id`；runner 默认使用账号元数据中的 `userId`，也可由 `input.userId` 覆盖。
- 这些接口只用于状态探针，不代表签到、活动 Pro 领取、抽奖参与或重置券使用会自动执行。

### 1.5.4 M05 side-effect 显式 probe

本轮通过生产 chunk 静态调用点、只读前置 GET 和无效/伪业务参数 POST 回放，确认以下 path、header 族和安全边界：

~~~text
GET  /api/commerce/activity/v1/sign-in/status?scene_codes=desktop_pet
POST /api/commerce/activity/v1/sign-in
GET  /api/commerce/benefit/v1/coupon/list?user_id=<user_id>&coupon_type=weekly_reset_coupon&offset=0&limit=50
POST /api/commerce/activity/v1/participate
GET  /api/commerce/product/v1/sku/usage-reset-coupon
GET  /api/commerce/lottery/v1/available-chances?user_id=<user_id>&activity_id=<activity_id>
GET  /api/commerce/lottery/v1/active-main-pools?activity_id=<activity_id>
GET  /api/commerce/activity/v1/lottery/chance-records?activity_id=<activity_id>&offset=0&limit=20
POST /api/commerce/lottery/v1/draw
~~~

签到状态 GET 返回 `sign_in_date` 和 `results[]`，其中桌宠场景使用 `desktop_pet`，`scene_codes` 是 repeated query。签到 POST body 为 `{ "request_no": "...", "scene_codes": ["desktop_pet"] }`，生产 chunk 使用 `trace-id` 和 `Content-Type: application/json`。`request_no` 是 caller-generated 短字符串；live 回放证明 UUID 形态过长会触发 422，短生产形态成功，因此实现和 CLI 都限制为非空且不超过 64 字符。

重置券列表 GET 使用 `coupon_type=weekly_reset_coupon`，offset 默认 0，limit 默认 50。reset-coupon activity participate body 为 `{ "user_id": "<user_id>", "request_no": "<caller-generated>" }`，生产 chunk 使用 `x-req-ctx`、`unique-uuid` 和 `Content-Type: application/json`。该 endpoint 与通用 activity participate 共用 path，但 fake user_id 也可返回 200，因此 200 不能直接解释为真实领取成功；当前也不能把它等同于“消耗已有重置券”。

2026-07-03 live 脱敏 evidence：

~~~text
output/tabbit-benefits-side-effects/side-effect-live-20260703T082200417Z.sanitized.json  # 过长 request_no，dailySignIn / reset-coupon participate 均 422
output/tabbit-benefits-side-effects/side-effect-live-20260703T082435232Z.sanitized.json  # 短 request_no，dailySignIn 成功，reset-coupon participate 返回 already_participated
~~~

成功样本中，`dailySignIn` 返回 `sign_in_result:"success"`，后续状态从 `signed_today:false` 变为 `signed_today:true`，`signed_days` 与 `total_signed_days` 递增。`participateResetCouponActivity` 返回 `{ activity_id: 10001, participation_result: "already_participated" }`，但重置券列表前后 hash 一致，quota usage 前后 hash 一致，因此仍不能接入默认 `useResetCoupon()` 维护链。

2026-07-04 live 聚合 evidence（写入 gitignored `tmp/live-fixtures`，不打印 raw fixture）：

- `dailySignIn` 重新捕获到已完成签到状态，`fixtures audit --scope benefits` 报告 `dailySignIn=ready`，但 Pro、真实重置券消耗和抽奖成功仍 missing。
- `verifySession` 对有效账号写入成功 fixture；对无效 cookie 账号写入 401/login_required fixture；`fixtures audit --scope session` 报告成功与过期 evidence 都 ready，但 `automated_session_refresh_strategy` 仍 blocked。
- 只读 commerce 探针显示当前账号有周重置券记录，但没有可用于 Pro/抽奖的 activity id；usage reset coupon SKU 仍返回不可购买/无效请求类失败，因此不能推导真实用券消耗 endpoint/body。
- `sendMessage` stream 探针仍返回 `protocol_changed` 类失败，没有生成真实上游 error-frame、取消或 backpressure evidence。

`fixtures audit --scope benefits --json` 会把上述每日签到成功计为 `successful_daily_sign_in_fixture`，也会把当天已完成状态 `signInResult:"already_signed"` 计为每日签到覆盖。2026-07-04 以后，`useResetCoupon` 成功 fixture 会把真实用券 coverage 计为 ready；仍为缺口的是 `successful_pro_activity_fixture` 和 `successful_lottery_draw_fixture`。这能防止把泛 activity participate、失败抽奖样本或非 M05 fixture 误判成可上线的副作用成功语义。活动 Pro coverage 只接受 participation/activity/claim/pro 专属成功字段；真实用券 coverage 接受 `useResetCoupon` 这类消费类 operation 的脱敏 endpoint/body/result hash 和 `usageResult` 等成功信号；`consumeResetCoupon` 仍只是离线 endpoint/body/result hash evidence 预检，不会触网用券。

抽奖相关 GET 需要 `activity_id`；当前账号的 `lottery/me` 返回 activity 不可用时，缺参 GET 会返回 422 validation error。`drawLottery()` 已校准 path 和 header 族，但有效 body 与成功响应仍应等拿到安全 evidence 后再提升为高层动作。`fixtures audit --scope benefits` 不会把泛 `ok/status/result:"success"` 视为抽奖成功；必须有 draw/lottery 专属成功字段或非空奖品/命中记录。

所有 POST 型方法在 `ProtocolTabbitClient` 中都要求 `confirmSideEffect:true`，否则触网前抛 `SIDE_EFFECT_CONFIRMATION_REQUIRED`。其中 `dailySignIn` 已通过默认 maintainer wrapper 显式传入确认并接入 `dailyCheckin`；其它 side-effect path 不改变默认 `maintain` 行为。

2026-07-04 auth endpoint 逆向新增 evidence：

- 直取 `https://web.tabbit.ai/login` 返回真实登录页 HTML，并预加载 `https://s0.meituan.net/mxx/yoda/yoda.seed.js`；浏览器实际执行时仍会把 `/login` 重写到产品页，所以动态 UI 触发不可作为唯一入口。
- 登录页专属 chunk `app/login/page-efa9456a2a9f4843.js` 证明发送验证码使用 `POST /proxy/v0/oauth/send-verification-code`，body keys 为 `uuid/platform/version/app/mobile`，常量为 `platform:"1"`、`version:""`、`app:"1000"`。
- 同一 chunk 证明提交短信码使用 `POST /proxy/v0/oauth/login`，body keys 为 `uuid/platform/version/app/mobile/smsCode`，可选 `channel`；Google 登录另走 `POST /proxy/v0/oauth/third-party-login`。
- `uuid` 是登录组件生成并复用的 64 位字母数字 auth client value，不是 Yoda 返回值；项目已在 `ProtocolTabbitClient` 中按该形状生成/复用，并把 auth probe 模板改为手机号协议占位 body。
- Yoda/captcha 成功仍 blocked：send-code 错误响应若含 `data.verifyUrl + data.requestCode`，浏览器调用 `window.YodaSeed({ requestCode, root:"verify-container" })`。尚未捕获完成 Yoda 后的验证码投递成功 fixture，也没有真实 `smsCode` 提交后的 session material fixture。

### 1.6 校准检查表

| 项目 | 验收 |
|---|---|
| sign-key | `GET /chat/sign-key` 返回稳定 key 字段或字符串；fixture 不泄露 cookie。 |
| 签名 payload | `timestamp.signature.sha256(bodyText)` 与浏览器 webpack 产物一致。 |
| 签名 header | `x-timestamp`、`x-nonce`、`x-signature`、`x-req-ctx`、`unique-uuid` 名称和值格式与真实请求一致。 |
| Cookie | 只来自 hydrated `cookieJarRef`，不写入账号元数据或 fixture。 |
| session verify | `GET /api/v0/user/base-info` 可用；2026-07-04 live fixture audit 已同时具备成功样本和 401/login_required 过期样本。fixture sanitizer 会屏蔽真实 `user_info.id`/`userId`；`fixtures audit --scope session` 可只读显示成功/过期 evidence，但在显式脱敏 `session_recovery_strategy` / `recoverSession` evidence 证明自动刷新或安全 re-auth recovery path 前，仍以 `automated_session_refresh_strategy` 标记 blocked。 |
| auth send/submit | endpoint 与 body 已推进到 `/proxy/v0/oauth/send-verification-code`、`/proxy/v0/oauth/login`、手机号 body 和 64 位 auth client uuid；Yoda 后验证码投递成功和提交短信码 session material 仍需脱敏 fixture，`fixtures audit --scope auth` 仍保持 blocked。 |
| quota usage | `GET /api/commerce/quota/v1/usage?user_id=...` 可用，不加 `x-signature/x-nonce`；成功样本含 `member_level`、`usage_percentage`、`current_cycle_end` 和 `unused_reset_coupon_count`，并归一化为 `tabbit-quota-usage`。 |
| 只读 commerce 探针 | lottery/newbie/placement/reward/lottery records GET 可用，不加 `x-signature/x-nonce`；newbie view_mode 枚举已校准，placement 使用 `placement_code`，记录查询需要 `user_id`。 |
| M05 side-effect probe | 签到状态、签到 POST、重置券列表/activity participate、抽奖机会/奖池/记录/抽奖 POST path 已校准；`dailySignIn` 短 `request_no` 成功证据已归档并可在显式 sign-in path 下进入默认 `dailyCheckin`；`fixtures audit --scope benefits` 可只读显示 Pro success、真实重置券消耗和抽奖 success 缺口；reset-coupon participate 仅证明 `already_participated` 活动状态；其它 POST 默认需要 `confirmSideEffect:true`，且不进入默认 `maintain`。 |
| upstream stream boundary | 本地 SSE parser、HTTP async flush、错误帧和取消基础测试已覆盖；`fixtures audit --scope upstream` 可只读显示真实上游错误帧、取消和 backpressure evidence 缺口，并要求样本同时带真实上游 marker 与 stream/SSE/NDJSON 元数据。本地 fake stream 和非流式 protocol probe 不计为真实上游校准。 |
| send body | `chat_session_id`、`content`、`selected_model`、`metadatas.html_content`、`references`、`entity` 字段与真实请求体对齐；已上传附件会生成 `references[].metadata.file_id`。 |
| 非流式响应 | 能归一化为 `contentBlocks:[{type:"text"}]` 或 `tool_use`。 |
| 流式文本 | SSE/NDJSON 可保留 delta 分片，不把 wire frame 当文本。 |
| 流式工具调用 | OpenAI `tool_calls` 或 Anthropic `tool_use/input_json_delta` 可输出官方风格 SSE。 |
| 错误分类 | 401/403/429/quota/protocol_changed 有稳定分类与账号状态动作。 |

## 2. Codex / Claude Code 真实端到端验证

### 2.1 启动本地网关

protocol-pool gateway 已可通过 `tabbit-pool serve/start` 启动；根项目 `tabbit2api start` 仍用于现有浏览器桥接网关。真实协议校准阶段建议优先跑 protocol-pool gateway，避免把 Playwright UI 桥接结果误判为纯协议结果。

统一本地参数：

~~~text
OpenAI base_url: http://127.0.0.1:50124/v1
Anthropic base_url: http://127.0.0.1:50124
API key: sk-tabbit-local
model: tabbit/priority
~~~

启动 protocol-pool gateway：

~~~powershell
node bin/tabbit-pool.js serve --host 127.0.0.1 --port 50124
~~~

命令会输出 `openaiBaseUrl` 和 `anthropicBaseUrl`，直到收到 Ctrl+C / SIGTERM 后关闭 server；输出不会包含 `TABBIT_POOL_API_KEY`、cookie 或 session。

先验证健康与模型：

~~~powershell
curl.exe http://127.0.0.1:50124/health
curl.exe -H "Authorization: Bearer sk-tabbit-local" http://127.0.0.1:50124/v1/models
~~~

也可以用内置 smoke 一次性检查 5 条本地兼容路由：

~~~powershell
node bin/tabbit-pool.js smoke gateway --json
~~~

该命令会向本地 gateway 发送最小请求：`/health`、`/v1/models`、`/v1/chat/completions`、`/v1/responses`、`/v1/messages`。如果 gateway 已连接真实 Tabbit 协议，后三个 POST 会触发上游消息请求；建议只在准备做端到端验收时执行。输出不会包含 API key、cookie 或 session。

### 2.2 Codex 验收

配置见根项目 `examples/codex/config.toml.example`。关键字段：

~~~toml
model_provider = "tabbit2api"
model = "tabbit/priority"
disable_response_storage = true

[model_providers.tabbit2api]
wire_api = "responses"
requires_openai_auth = true
base_url = "http://127.0.0.1:50124/v1"
env_key = "TABBIT_API_KEY"
~~~

环境变量：

~~~powershell
$env:TABBIT_API_KEY = "sk-tabbit-local"
~~~

真实 `/api/v1/chat/completion` 文本链路验收建议在启动 gateway 前显式开启：

~~~powershell
$env:TABBIT_POOL_COMPAT_STRIP_CLIENT_TOOLS = "true"
~~~

该开关只用于剥离 Codex 自动附带的已知客户端内置协作工具，避免文本验收请求因未校准原生工具字段失败；默认关闭，不代表真实上游工具语义已支持。

验收任务建议：

0. 本地路由：先确认 `tabbit-pool smoke gateway --json` 返回 `status:"ok"`。
1. 纯文本：让 Codex 返回唯一 marker 或总结一个小文件。
2. 流式：观察首 token / 首事件是否及时返回。
3. 工具字段边界：真实 `/api/v1/chat/completion` 已校准为不支持原生工具字段；工具调用验收暂保留在兼容层/fixture，不作为真实协议 readiness 的通过条件。

通过标准：Codex 不需要知道 Tabbit 账号；只使用 `base_url + key + model` 即可完成一次真实代码任务。通过后记录本地验收标记：

~~~powershell
node bin/tabbit-pool.js readiness mark --codex-verified --json
~~~

### 2.3 Claude Code 验收

PowerShell：

~~~powershell
$env:ANTHROPIC_BASE_URL = "http://127.0.0.1:50124"
$env:ANTHROPIC_API_KEY = "sk-tabbit-local"
$env:ANTHROPIC_MODEL = "tabbit/priority"
claude
~~~

做真实协议文本链路验收时，同样建议用 `TABBIT_POOL_COMPAT_STRIP_CLIENT_TOOLS=true` 启动 gateway；Claude Code 的 `--tools ""` 会发送空工具数组，gateway 会把它归一化为无工具请求。

验收任务建议：

0. 本地路由：先确认 `tabbit-pool smoke gateway --json` 返回 `status:"ok"`。
1. 纯文本：让 Claude Code 返回唯一 marker 或解释一个小模块。
2. 流式：确认 Anthropic SSE 输出 `message_start`、`content_block_delta`、`message_stop`。
3. 工具字段边界：真实上游原生工具字段已有 unsupported evidence，暂不作为文本端到端 readiness 的通过条件；需要自动执行工具时应进入本地 loop 设计。

通过标准：Claude Code 不需要真实 Anthropic key，只使用本地 `ANTHROPIC_BASE_URL` 和 `sk-tabbit-local`。通过后记录本地验收标记：

~~~powershell
node bin/tabbit-pool.js readiness mark --claude-verified --json
~~~

## 3. 是否需要网关本地工具执行 loop

当前结论：**先不默认做本地 loop，优先保持官方协议结构；需要时用受控 opt-in loop。**

运行时策略由 `TABBIT_POOL_TOOL_LOOP_MODE` / `config.compat.toolLoopMode` 表达，默认 `client_executes_tools_first`。可显式设为 `disabled`，表示剥离工具字段；也可设为 `local_executes_tools`，但只有 gateway 宿主注入 `executeLocalToolUse` / `localToolExecutor.execute` 时才会执行本地工具。真实 `/api/v1/chat/completion` 原生工具字段仍返回 `TOOL_FIELDS_UNSUPPORTED`。本地执行 guardrails 来自 `TABBIT_POOL_LOCAL_TOOL_ALLOWLIST`、`TABBIT_POOL_LOCAL_TOOL_MAX_ROUNDS`、`TABBIT_POOL_LOCAL_TOOL_TIMEOUT_MS`、`TABBIT_POOL_LOCAL_TOOL_MAX_RESULT_CHARS`。

| 场景 | 是否需要本地 loop | 理由 |
|---|---|---|
| Codex / Claude Code 自己执行工具 | 不需要 | 客户端会收到 tool call、执行工具、再把 tool result 发回；网关只要保真即可。 |
| 普通 SDK 调用方自己执行 function | 不需要 | 与 OpenAI / Anthropic 官方 API 使用方式一致。 |
| 客户端只会发 tools，但不会执行工具 | 需要 | 使用 `local_executes_tools`，网关 wrapper 执行注入工具并继续多轮，否则会停在 tool call。 |
| Assistants 风格 requires_action | 视接口而定 | 根项目已有 Assistants 工具流程，可作为后续迁移参考。 |
| 想内置受控工具，例如 fetch / js / shell | 需要但要单独设计 | 当前只提供注入 executor 和 guardrails，不内置任何高风险工具；内置工具仍需单独安全设计。 |

本地 loop 的验收口径：

1. 工具白名单明确，不执行未知工具。
2. 总轮数有限制，默认 4，可通过 env 覆盖。
3. 单工具执行可配置超时，超时返回 `timeout/LOCAL_TOOL_TIMEOUT`。
4. 工具输出会截断；具体脱敏由宿主 executor 或外围日志层负责。
5. OpenAI Chat、Responses、Anthropic Messages 三套入口经同一 runner wrapper 覆盖测试。
6. 客户端已有工具执行能力时，可以关闭本地 loop，避免重复执行。

## 4. 真实 403 响应体 fixture 细分

当前代码新增 `classifyForbiddenSignal()`，把 403 细分为：

| kind | 典型信号 | 建议账号动作 |
|---|---|---|
| `risk_control` | risk、abuse、suspicious、captcha、security、风控、人机验证 | `suspect`，隔离账号，避免立即重试。 |
| `signature_or_protocol` | signature、sign-key、timestamp、nonce、hmac、payload、签名 | `protocol_probe`，优先校准签名/请求体，不轮询账号。 |
| `session_or_cookie` | cookie、session、login、auth、expired、token、登录、会话 | `login_expired`，刷新或导入 session。 |
| `entitlement_or_model` | premium、pro、plan、tier、permission、model、权限、权益 | `cooldown` 或切换模型/账号权益。 |
| `unknown_forbidden` | 只有 Forbidden 或无结构 body | `suspect`，先抓 fixture。 |

CLI 可直接带上下文获取建议，输出会脱敏：

~~~powershell
node bin/tabbit-pool.js probe advice --status 403 --message "<403 response message>" --json
~~~

输出示例：

~~~json
{
  "category": "forbidden",
  "severity": "error",
  "recommendation": "Isolate this account...",
  "forbidden": {
    "kind": "risk_control",
    "accountAction": "suspect",
    "retryable": false
  }
}
~~~

真实 403 fixture 校准时必须记录：

- HTTP status
- response body 的脱敏 `code/message/reason/type`
- 当前 operation：`sendMessage` / `verifySession` / `listModels`
- 是否同账号复现
- 是否多账号复现
- 是否同一签名 payload 可复现
- 账号状态动作建议

## 完成定义

这 4 项全部完成时，应满足：

- 至少一个真实 `verifySession` 成功 fixture 进入回归测试。
- 至少一个真实 `sendMessage` 成功 fixture 进入回归测试。
- 至少一个真实流式文本 fixture 进入回归测试。
- 至少一个真实工具调用 fixture 或明确“Tabbit 上游不支持原生工具字段”的 fixture 进入回归测试。
- Codex 完成一次代码任务，并记录配置与结果，同时 `readiness mark --codex-verified` 已写入本地验收标记。
- Claude Code 完成一次代码任务，并记录配置与结果，同时 `readiness mark --claude-verified` 已写入本地验收标记。
- 403 至少有一个真实响应体 fixture，并能归入上述 kind 之一。
- 如启用默认权益维护，quota usage 和每日签到 maintain 结果已使用脱敏 evidence 验证；只读 commerce 探针和其它 M05 side-effect probe 可通过 `probe protocol` 单独执行；POST 型 side-effect operation 需要 `confirmSideEffect:true`，默认 maintain 仍不触发活动 Pro 领取、抽奖或真实用券。
- 文档、测试、API 兼容层同步更新，`npm test` 通过。

## 相关文档

- [Codex Claude 与三方工具接入](11-Codex-Claude与三方工具接入.md)
- [账号风控与 403 排障](12-账号风控与403排障.md)
- [流式 SSE 链路](10-流式SSE链路.md)
- [API 文档](07-API文档.md)
- [M08 观测运维](modules/M08-观测运维/_M08-观测运维.md)
